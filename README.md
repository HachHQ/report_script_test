Консольная утилита для обработки CSV-файлов с данными о сотрудниках и формирования аналитических отчетов.
В данный момент реализован отчет **Performance**, который рассчитывает среднюю эффективность сотрудников в разрезе их должностей.

## 🛠 Установка
Проект использует [Poetry](https://python-poetry.org/) для управления зависимостями.
1. Клонируйте репозиторий.
2. Установите зависимости:
```bash
poetry install
```

## 🚀 Запуск
Скрипт принимает на вход один или несколько CSV-файлов и название отчета.
**Общий синтаксис:**
```bash
poetry run python -m src.main --files <путь_к_файлу_1> [путь_к_файлу_2 ...] --report <тип_отчета>
```
**Пример запуска:**
```bash
poetry run python -m src.main --files test_data/* --report performance
```

Аргументы:
*   `--files`: Пути к CSV-файлам (можно указать несколько через пробел).
*   `--report`: Тип отчета (сейчас доступен `performance`).

## 🧪 Тестирование
Код покрыт тестами с использованием `pytest`. Для запуска тестов и проверки покрытия выполните команду:
```bash
poetry run pytest tests/ --cov=src
```

## ℹ️ Для разработчиков (Архитектура)
Проект построен с учетом принципов **SOLID** для легкого масштабирования.
**Как добавить новый отчет:**
1. Создайте новый класс отчета в `src/reports.py`, унаследовав его от `BaseReport`.
2. Реализуйте методы:
   *   `generate(self, data)`: логика обработки данных.
   *   `headers(self)`: список заголовков для таблицы.
3. Добавьте новый класс в словарь `REPORT_REGISTRY` в конце файла `src/reports.py`.

После этого новый отчет станет доступен для вызова через аргумент `--report`.